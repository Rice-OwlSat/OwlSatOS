# The OwlSat Kernel
---
This is our implementation of the OwlSat kernel in C/C++ for the RP2350 CPU.

## Building
Open the project in VS Code
Make sure you have Pico SDK extension installed
Run the `compile` button at the bottom of the screen
Copy the `.uf2` file from the `build/` directory into the boot selector of the board

